import React from "react";

const Navbar = () => {
    return (
        <div>
            <main>
                <nav className="bg-gray-900 flex justify-center py-6">
                    <p className="text-gray-100 font-bold text-4xl">Data Query Application</p>
                </nav>
            </main>
        </div>
    );
};

export default Navbar;
